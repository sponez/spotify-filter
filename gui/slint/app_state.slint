export enum AppStateEnum {
    Login,
    AwaitLogin,
    SignedIn,
    ErrorLogin,
    Settings
}

// Mirrors application::settings::AdditionalFilterAction.
// Int mapping: 0=None_, 1=AddToPlaylist, 2=MoveToPlaylist
export enum FilterActionEnum {
    None_,
    AddToPlaylist,
    MoveToPlaylist
}