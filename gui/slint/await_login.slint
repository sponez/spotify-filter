import { Spinner } from "std-widgets.slint";

export component LoginInProcessView {
    width: 360px;
    height: 240px;
    
    VerticalLayout {
        padding: root.width * 25%;
        Spinner { indeterminate: true; }
    }
}